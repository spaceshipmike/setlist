{
  "project": "fctry",
  "config_label": "flash-lite@t0.0",
  "model": "google/gemini-2.5-flash-lite",
  "temperature": 0,
  "sample": 2,
  "digest": "fctry is a Claude Code plugin that orchestrates eight specialized agents to enable autonomous software development from experience-first specifications (NLSpec v2). It converts conversational descriptions of user experiences into complete NLSpec v2 documents, generates scenario holdout sets, and manages the build-measure-learn loop until scenario satisfaction is achieved. The system enforces an \"experience language everywhere\" principle, ensuring user-facing surfaces speak plain English while blocking factory-internal vocabulary. A dedicated next-action recommender (`/fctry:next`) reads cached state to instantly suggest the highest-priority action. The spec viewer provides a recursive kanban interface for visual prioritization, auto-generated diagrams to reveal connections, and dark mode for comfort. Designed for non-coders, it aims to translate vision directly into working software without requiring coding knowledge or implementation decisions.\n\nThe core flow begins with `/fctry:init`, where the State Owner assesses the project's current state (greenfield or existing code/docs), followed by an interview conducted by the Interviewer agent. The Interviewer asks clarifying questions based on user responses, steering the conversation toward four experience dimensions: Vision Completeness, Flow Coverage, Edge Case Awareness, and Success Definition. The interview state is saved automatically upon pausing (`save and pause`), allowing multi-session continuity. Upon completion, the Scenario Crafter generates scenarios, and the Spec Writer synthesizes the interview, scenarios, and State Owner briefing into `.fctry/spec.md` and `.fctry/scenarios.md`. It also generates project synopsis frontmatter (short, medium, README descriptions, tech stack, patterns, goals) and evergreen project instructions in `CLAUDE.md`. The version registry in `.fctry/config.json` is seeded with default external (0.1.0) and spec (0.1) versions.\n\nEvolving an existing spec is handled by `/fctry:evolve <section>`, which targets specific sections for refinement. The system detects drift between the spec and codebase, presenting conflicts for user resolution before proceeding. References are incorporated via `/fctry:ref`, which interprets external content (URLs, designs) in experience language and adds them to section 5.2 (Experience References). The system also supports overnight research via `/fctry:ref` (no arguments), which analyzes gaps and presents actionable patterns and recommendations.\n\n`/fctry:review` performs a deep state assessment, identifying spec-code divergence, missing implementations, guardrail violations, and experience degradations. Findings are categorized by action orientation (Decisions Needed, Ready to Build) and presented with clear recommendations. CLAUDE.md is also audited for drift.\n\n`/fctry:execute` initiates autonomous builds. The Executor proposes a plan based on scenario satisfaction and section readiness, chunking work into discrete units. The plan includes phase type (Capability, Hardening, Refactor, Integration, Polish) and cognitive tier. User priorities (Speed, Token Efficiency, Reliability) shape the execution strategy. Builds are plan-gated; once approved, execution is autonomous, handling failures and rearchitecting silently. The system resurfaces only for experience-level questions where the spec is ambiguous. Post-chunk verification by the Observer ensures code quality and scope compliance. A mandatory post-build retrospective extracts lessons into `.fctry/lessons.md`. The system manages versioning via the `.fctry/config.json` registry, auto-tagging commits and propagating versions to declared targets. The final output is an experience report detailing what the user can now do, not a satisfaction scorecard.\n\nThe live spec viewer, accessible via `/fctry:view` or auto-starting on first use, provides a multi-project dashboard and a spec reader. It updates specs in real-time via WebSocket, highlights active sections, displays change history, and supports dark mode. During builds, it transforms into mission control, showing an interactive dependency graph of chunks, real-time progress, and an activity feed. The viewer also hosts an async inbox for evolve ideas, references, and feature requests, processed in the background.\n\nHard constraints include operating solely as a Claude Code plugin, using experience language exclusively in user-facing output, maintaining scenario holdout separation, requiring plan-level approval before execution, always consulting the State Owner first, and prohibiting human code review. The system enforces its workflow deterministically, uses structured choices via `AskUserQuestion` for discrete decisions, and provides a terminal status line with visual progress bars and derived next-action recommendations.\n\nThe system tracks entities like the spec document, scenarios, interview state, changelog, build plans, build runs, version registry, project instructions (CLAUDE.md), project registry, viewer state, verification verdicts, observation reports, build traces, experience questions, phase types, workflow state, the spec index (SQLite), section readiness, section priority, diagram definitions, untracked changes, and the async inbox queue. Global state like the project registry, global config, viewer runtime files, and memory store reside in `~/.fctry/`.\n\nExternal connections include GitHub, Firecrawl, Context7/DeepWiki MCP, Playwright MCP, Chrome DevTools MCP, Surf, OpenBrowser MCP, Showboat, Peekaboo, and local filesystem/SQLite. These are used for reference fetching, build observation, and local state management. The system prioritizes local inference and graceful degradation when external services are unavailable.\n\nPerformance expectations are strict: commands should respond within 2-5 seconds, interviews should feel conversational, spec generation within 90 seconds, build plans within 60 seconds, and viewer rendering within 1 second. The system aims for a responsive, conversational feel, never leaving the user waiting without feedback.\n\nThe system's scope is limited to single-user, local-first development within Claude Code. It does not cover collaboration, cloud deployment, external project management tool integration, or git repository initialization.\n\nKey anti-patterns avoided include becoming an IDE or project management tool, silently overwriting user data, assuming on conflicts, allowing direct spec editing in the viewer, skipping tool validation, drive-by refactoring during feature work, and the mutable-edge-adapter pattern where agents invent new primitives mid-build.\n\nThe system's convergence strategy progresses from core spec authoring to autonomous building, then to enhanced observability and user experience features like the viewer's mission control and async inbox, culminating in a viewer-as-control-plane architecture and an optional fully-autonomous loop. This phased approach ensures core functionality is robust before adding complexity.fctry is a Claude Code plugin that orchestrates eight specialized agents to enable autonomous software development from experience-first specifications (NLSpec v2). It converts conversational descriptions of user experiences into complete NLSpec v2 documents, generates scenario holdout sets, and manages the build-measure-learn loop until scenario satisfaction is achieved. The system enforces an \"experience language everywhere\" principle, ensuring user-facing surfaces speak plain English while blocking factory-internal vocabulary. A dedicated next-action recommender (`/fctry:next`) reads cached state to instantly suggest the highest-priority action. The spec viewer provides a recursive kanban interface for visual prioritization, auto-generated diagrams to reveal connections, and dark mode for comfort. Designed for non-coders, it aims to translate vision directly into working software without requiring coding knowledge or implementation decisions.\n\nThe core flow begins with `/fctry:init`, where the State Owner assesses the project's current state (greenfield or existing code/docs), followed by an interview conducted by the Interviewer agent. The Interviewer asks clarifying questions based on user responses, steering the conversation toward four experience dimensions: Vision Completeness, Flow Coverage, Edge Case Awareness, and Success Definition. The interview state is saved automatically upon pausing (`save and pause`), allowing multi-session continuity. Upon completion, the Scenario Crafter generates scenarios, and the Spec Writer synthesizes the interview, scenarios, and State Owner briefing into `.fctry/spec.md` and `.fctry/scenarios.md`. It also generates project synopsis frontmatter (short, medium, README descriptions, tech stack, patterns, goals) and evergreen project instructions in `CLAUDE.md`. The version registry in `.fctry/config.json` is seeded with default external (0.1.0) and spec (0.1) versions.\n\nEvolving an existing spec is handled by `/fctry:evolve <section>`, which targets specific sections for refinement. The system detects drift between the spec and codebase, presenting conflicts for user resolution before proceeding. References are incorporated via `/fctry:ref`, which interprets external content (URLs, designs) in experience language and adds them to section 5.2 (Experience References). The system also supports overnight research via `/fctry:ref` (no arguments), which analyzes gaps and presents actionable patterns and recommendations.\n\n`/fctry:review` performs a deep state assessment, identifying spec-code divergence, missing implementations, guardrail violations, and experience degradations. Findings are categorized by action orientation (Decisions Needed, Ready to Build) and presented with clear recommendations. CLAUDE.md is also audited for drift.\n\n`/fctry:execute` initiates autonomous builds. The Executor proposes a plan based on scenario satisfaction and section readiness, chunking work into discrete units. The plan includes phase type (Capability, Hardening, Refactor, Integration, Polish) and cognitive tier. User priorities (Speed, Token Efficiency, Reliability) shape the execution strategy. Builds are plan-gated; once approved, execution is autonomous, handling failures and rearchitecting silently. The system resurfaces only for experience-level questions where the spec is ambiguous or contradictory. Post-chunk verification by the Observer ensures code quality and scope compliance. A mandatory post-build retrospective extracts lessons into `.fctry/lessons.md`. The system manages versioning via the `.fctry/config.json` registry, auto-tagging commits and propagating versions to declared targets. The final output is an experience report detailing what the user can now do, not a satisfaction scorecard.\n\nThe live spec viewer, accessible via `/fctry:view` or auto-starting on first use, provides a multi-project dashboard and a spec reader. It updates specs in real-time via WebSocket, highlights active sections, displays change history, and supports dark mode. During builds, it transforms into mission control, showing real-time progress: the active chunk, completed sections, dependency status, and pending experience questions. The viewer also hosts an async inbox for evolve ideas, references, and new feature proposals, processed in the background.\n\nHard constraints include operating solely as a Claude Code plugin, using experience language exclusively in user-facing output, maintaining scenario holdout separation, requiring plan-level approval before execution, always consulting the State Owner first, and prohibiting human code review. The system enforces its workflow deterministically, uses structured choices via `AskUserQuestion` for discrete decisions, and provides a terminal status line with visual progress bars and derived next-action recommendations.\n\nThe system tracks entities like the spec document, scenarios, interview state, changelog, build plans, build runs, version registry, project instructions (CLAUDE.md), project registry, viewer state, verification verdicts, observation reports, build traces, experience questions, phase types, workflow state, the spec index (SQLite), section readiness, section priority, diagram definitions, untracked changes, and the async inbox queue. Global state like the project registry, global config, viewer runtime files, and memory store reside in `~/.fctry/`.\n\nExternal connections include GitHub, Firecrawl, Context7/DeepWiki MCP, Playwright MCP, Chrome DevTools MCP, Surf, OpenBrowser MCP, Showboat, Peekaboo, and local filesystem/SQLite. These are used for reference fetching, build observation, and local state management. The system prioritizes local inference and graceful degradation when external services are unavailable.\n\nPerformance expectations are strict: commands should respond within 2-5 seconds, interviews should feel conversational, spec generation within 90 seconds, build plans within 60 seconds, and viewer rendering within 1 second. The system aims for a responsive, conversational feel, never leaving the user waiting without feedback.\n\nThe system's scope is limited to single-user, local-first development within Claude Code. It does not cover collaboration, cloud deployment, external project management tool integration, or git repository initialization.\n\nKey anti-patterns avoided include becoming an IDE or project management tool, silently overwriting user data, assuming on conflicts, allowing direct spec editing in the viewer, skipping tool validation, drive-by refactoring during feature work, and the mutable-edge-adapter pattern where agents invent new primitives mid-build.fctry is a Claude Code plugin that orchestrates eight specialized agents to enable autonomous software development from experience-first specifications (NLSpec v2). It converts conversational descriptions of user experiences into complete NLSpec v2 documents, generates scenario holdout sets, and manages the build-measure-learn loop until scenario satisfaction is achieved. The system enforces an \"experience language everywhere\" principle, ensuring user-facing surfaces speak plain English while blocking factory-internal vocabulary. A dedicated next-action recommender (`/fctry:next`) reads cached state to instantly suggest the highest-priority action. The spec viewer provides a recursive kanban interface for visual prioritization, auto-generated diagrams to reveal connections, and dark mode for comfort. Designed for non-coders, it aims to translate vision directly into working software without requiring coding knowledge or implementation decisions.\n\nThe core flow begins with `/fctry:init`, where the State Owner assesses the project's current state (greenfield or existing code/docs), followed by an interview conducted by the Interviewer agent. The Interviewer asks clarifying questions based on user responses, steering the conversation toward four experience dimensions: Vision Completeness, Flow Coverage, Edge Case Awareness, and Success Definition. The interview state is saved automatically upon pausing (`save and pause`), allowing multi-session continuity. Upon completion, the Scenario Crafter generates scenarios, and the Spec Writer synthesizes the interview, scenarios, and State Owner briefing into `.fctry/spec.md` and `.fctry/scenarios.md`. It also generates project synopsis frontmatter (short, medium, README descriptions, tech stack, patterns, goals) and evergreen project instructions in `CLAUDE.md`. The version registry in `.fctry/config.json` is seeded with default external (0.1.0) and spec (0.1) versions.\n\nEvolving an existing spec is handled by `/fctry:evolve <section>`, which targets specific sections for refinement. The system detects drift between the spec and codebase, presenting conflicts for user resolution before proceeding. References are incorporated via `/fctry:ref`, which interprets external content (URLs, designs) in experience language and adds them to section 5.2 (Experience References). The system also supports overnight research via `/fctry:ref` (no arguments), which analyzes gaps and presents actionable patterns and recommendations.\n\n`/fctry:review` performs a deep state assessment, identifying spec-code divergence, missing implementations, guardrail violations, and experience degradations. Findings are categorized by action orientation (Decisions Needed, Ready to Build) and presented with clear recommendations. CLAUDE.md is also audited for drift.\n\n`/fctry:execute` initiates autonomous builds. The Executor proposes a plan based on scenario satisfaction and section readiness, chunking work into discrete units. The plan includes phase type (Capability, Hardening, Refactor, Integration, Polish) and cognitive tier. User priorities (Speed, Token Efficiency, Reliability) shape the execution strategy. Builds are plan-gated; once approved, execution is autonomous, handling failures and rearchitecting silently. The system resurfaces only for experience-level questions where the spec is ambiguous or contradictory. Post-chunk verification by the Observer ensures code quality and scope compliance. A mandatory post-build retrospective extracts lessons into `.fctry/lessons.md`. The system manages versioning via the `.fctry/config.json` registry, auto-tagging commits and propagating versions to declared targets. The final output is an experience report detailing what the user can now do, not a satisfaction scorecard.\n\nThe live spec viewer, accessible via `/fctry:view` or auto-starting on first use, provides a multi-project dashboard and a spec reader. It updates specs in real-time via WebSocket, highlights active sections, displays change history, and supports dark mode. During builds, it transforms into mission control, showing real-time progress: the active chunk, completed sections, dependency status, and pending experience questions. The viewer also hosts an async inbox for evolve ideas, references, and new feature proposals, processed in the background.\n\nHard constraints include operating solely as a Claude Code plugin, using experience language exclusively in user-facing output, maintaining scenario holdout separation, requiring plan-level approval before execution, always consulting the State Owner first, and prohibiting human code review. The system enforces its workflow deterministically, uses structured choices via `AskUserQuestion` for discrete decisions, and provides a terminal status line with visual progress bars and derived next-action recommendations.\n\nThe system tracks entities like the spec document, scenarios, interview state, changelog, build plans, build runs, version registry, project instructions (CLAUDE.md), project registry, viewer state, verification verdicts, observation reports, build traces, experience questions, phase types, workflow state, the spec index (SQLite), section readiness, section priority, diagram definitions, untracked changes, and the async inbox queue. Global state like the project registry, global config, viewer runtime files, and memory store reside in `~/.fctry/`.\n\nExternal connections include GitHub, Firecrawl, Context7/DeepWiki MCP, Playwright MCP, Chrome DevTools MCP, Surf, OpenBrowser MCP, Showboat, Peekaboo, and local filesystem/SQLite. These are used for reference fetching, build observation, and local state management. The system prioritizes local inference and graceful degradation when external services are unavailable.\n\nPerformance expectations are strict: commands should respond within 2-5 seconds, interviews should feel conversational, spec generation within 90 seconds, build plans within 60 seconds, and viewer rendering within 1 second. The system aims for a responsive, conversational feel, never leaving the user waiting without feedback.\n\nThe system's scope is limited to single-user, local-first development within Claude Code. It does not cover collaboration, cloud deployment, external project management tool integration, or git repository initialization.\n\nKey anti-patterns avoided include becoming an IDE or project management tool, silently overwriting user data, assuming on conflicts, allowing direct spec editing in the viewer, skipping tool validation, drive-by refactoring during feature work, and the mutable-edge-adapter pattern where agents invent new primitives mid-build.\n\n---\n\n## Appendix A: Decision Rationale\n\n**Why experience language only, never implementation language?**\nThe target user is a non-coder with a clear vision but no mental model of databases, APIs, or architecture. If the spec uses technical language, the user cannot meaningfully review or refine it. Experience language (\"the user sees a list sorted by urgency\") is accessible, reviewable, and precise enough for a coding agent to infer implementation. This decision rules out any UI that asks the user about technical details.\n\n**Why are scenarios in a separate file from the spec?**\nIn machine learning, holdout sets are kept separate from training data to prevent overfitting. The same principle applies here: if scenarios are visible to the coding agent during development, the agent can \"teach to the test\" by implementing exactly what the scenarios check and nothing more. Separation ensures the agent builds from the spec (the experience) and scenarios validate the result, not guide it.\n\n**Why must the State Owner always run first?**\nWithout grounding in reality, spec updates become speculative. The State Owner provides the current state: what code exists, what the spec says, where they diverge, what changed recently. Every other agent operates on this briefing. If the State Owner runs later (or not at all), agents work from assumptions, leading to specs that describe things that don't exist or ignore things that do.\n\n**Why addressable sections with both aliases and numbers?**\nHumans remember concepts, not numbers. \"The core flow\" is easier to recall than \"section 2.2.\" But numbers provide stable, unambiguous references that survive renames. Supporting both gives users the flexibility to reference sections however they think about them, without the system needing to guess or resolve ambiguity.\n\n**Why plan-level approval instead of per-chunk gates?**\nThe user needs to control the scope and direction of builds, but not the moment-to-moment execution. Plan approval gives the user authority over what gets built, how much work is in scope, and the execution strategy. Per-chunk gates added friction (\"chunk 1 done, continue?\") without adding value — the user always said yes. After plan approval, the system executes autonomously, handling technical decisions silently. The user retains control through the plan itself and through experience questions when the spec is ambiguous.\n\n**Why multi-session interviews?**\nReal projects are complex. Users need time to think, gather information, or consult others before answering some questions. Forcing completion in one session leads to shallow specs or abandoned interviews. Multi-session support respects the user's time and thinking process, making spec authoring a progressive activity rather than a marathon session.\n\n**Why enforce the workflow instead of just documenting it?**\nDocumentation describes intent; enforcement ensures adherence. When the process is only documented, Claude can (and does) skip steps — going straight to code when a quick fix seems obvious, bypassing the State Owner scan, updating code without updating the spec. For a non-coder, this is invisible: they think they're working within the factory model, but they're actually in ad-hoc mode. Enforcement makes the boundary explicit. The numbered-options error (\"Run State Owner now / Skip / Abort\") keeps it conversational rather than rigid — the user can always skip, but they do so consciously.\n\n**Why SQLite as a cache instead of as the primary store?**\nThe markdown spec must remain portable and human-readable. A non-coder should be able to open the spec in any text editor or viewer. Making SQLite the primary store would lock the spec behind a database that requires tooling to read. By keeping markdown as the source of truth and SQLite as a derived cache, the system gets structured queries (section-level access, cross-references, readiness filtering) without sacrificing portability. If the database file is deleted, corrupted, or missing, agents fall back to reading the full markdown file — the system degrades gracefully rather than failing.\n\n**Why detect untracked changes via a hook instead of only during review?**\nNon-coders may not realize that fixing a bug directly in code creates drift between the spec and the implementation. By the time they run `/fctry:review`, the drift may have compounded across multiple files. Real-time detection surfaces the issue immediately with a gentle nudge — not a blocking error. The user can dismiss it and reconcile later, but they're aware. This is designed to be dialed back: if the nudge proves too intrusive, it can be moved to `/fctry:review`-only detection without changing the spec.\n\n**Why autonomous execution after plan approval instead of per-chunk approval gates?**\nPer-chunk gates were designed to prevent unbounded resource consumption, but in practice they interrupt the user for decisions that don't require human judgment — \"chunk 1 done, continue to chunk 2?\" The user always says yes. The meaningful decision is the plan itself: what scope of work, which scenarios, what parallelization strategy. Once that's approved, the remaining gates add friction without adding value. The user still controls scope (through the plan) and can still observe progress (through mission control). They lose the ability to pause mid-build, but gain uninterrupted autonomous progress. Code failures, retries, and rearchitecting are the agent's domain — surfacing them breaks the factory model's core promise that no human touches or reviews the code.\n\n**Why a spec viewer that auto-starts silently instead of requiring `/fctry:view`?**\nObservability should be always available, not opt-in. The viewer runs on a plugin hook that fires on every prompt, but the `ensure` logic makes it a no-op (<5ms) when no spec exists or the viewer is already running — so it never slows anything down. Auto-start uses `--no-open` to avoid surprise browser tabs; the user runs `/fctry:view` when they want to actually look at the viewer. The server persists across sessions because it serves all projects — killing it on one session end would disrupt monitoring of other projects. Self-healing on the next prompt means no orphaned-process management needed. The result: the viewer is always ready when the user wants it, never in the way when they don't.\n\n**Why a single multi-project server instead of per-project servers?**\nA user with many projects (the target persona) would otherwise accumulate separate server processes, each on a different port, each requiring its own browser tab. That's operational overhead that contradicts the \"co-founder with perfect memory\" vision. A single server with a project sidebar lets the user see all their projects at a glance and switch instantly — the same mental model as a multi-tab IDE or a project management dashboard. It also creates the natural foundation for a future dashboard view (overview of all projects) and for the viewer evolving into a control plane. The tradeoff is increased server complexity and a global lifecycle (persists across sessions, must self-heal), but the user experience is dramatically simpler.\n\n---\n\n## Appendix A: Decision Rationale\n\n**Why experience language only, never implementation language?**\nThe target user is a non-coder with a clear vision but no mental model of databases, APIs, or architecture. If the spec uses technical language, the user cannot meaningfully review or refine it. Experience language (\"the user sees a list sorted by urgency\") is accessible, reviewable, and precise enough for a coding agent to infer implementation. This decision rules out any UI that asks the user about technical details.\n\n**Why are scenarios in a separate file from the spec?**\nIn machine learning, holdout sets are kept separate from training data to prevent overfitting. The same principle applies here: if scenarios are visible to the coding agent during development, the agent can \"teach to the test\" by implementing exactly what the scenarios check and nothing more. Separation ensures the agent builds from the spec (the experience) and scenarios validate the result, not guide it.\n\n**Why must the State Owner always run first?**\nWithout grounding in reality, spec updates become speculative. The State Owner provides the current state: what code exists, what the spec says, where they diverge, what changed recently. Every other agent operates on this briefing. If the State Owner runs later (or not at all), agents work from assumptions, leading to specs that describe things that don't exist or ignore things that do.\n\n**Why addressable sections with both aliases and numbers?**\nHumans remember concepts, not numbers. \"The core flow\" is easier to recall than \"section 2.2.\" But numbers provide stable, unambiguous references that survive renames. Supporting both gives users the flexibility to reference sections however they think about them, without the system needing to guess or resolve ambiguity.\n\n**Why plan-level approval instead of per-chunk gates?**\nThe user needs to control the scope and direction of builds, but not the moment-to-moment execution. Plan approval gives the user authority over what gets built, how much work is in scope, and the execution strategy. Per-chunk gates added friction (\"chunk 1 done, continue?\") without adding value — the user always said yes. After plan approval, the system executes autonomously, handling failures and rearchitecting silently. The user retains control through the plan itself and through experience questions when the spec is ambiguous.\n\n**Why multi-session interviews?**\nReal projects are complex. Users need time to think, gather information, or consult others before answering some questions. Forcing completion in one session leads to shallow specs or abandoned interviews. Multi-session support respects the user's time and thinking process, making spec authoring a progressive activity rather than a marathon session.\n\n**Why enforce the workflow instead of just documenting it?**\nDocumentation describes intent; enforcement ensures adherence. When the process is only documented, Claude can (and does) skip steps — going straight",
  "token_count": 7631,
  "matched_features": [
    "NLSpec",
    "Claude Code plugin",
    "scenarios",
    "Executor",
    "Observer",
    "Spec Writer",
    "State Owner",
    "viewer",
    "kanban"
  ],
  "missing_features": [
    "Voice Translator"
  ],
  "recall": 0.9,
  "length_compliance": "over_ceiling",
  "duration_ms": 20548,
  "cost_usd": null,
  "error": null
}